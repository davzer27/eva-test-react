'use strict';
import React from 'react';

//wrapper for the landing page
const Details = (props) => {
  const { history} = props
  return (
    <div>
      The username of the selected profile is :
    </div>
  );
}

export default Details;
